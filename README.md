# quiztador-common

> **Moved.** The source now lives in [`PiJaAB/quiztador-monorepo`](https://github.com/PiJaAB/quiztador-monorepo)
> under `packages/shared`, published in the workspace as `@quiztador/shared`.
>
> **Do not commit here.** A change made in this repository is a silent fork: the app no
> longer reads it, and nothing will notice the two copies drifting apart.
>
> The `build` branch stays at the last published artefact. Firebase Functions, web and the
> dashboard still consume it from that pin and keep working until they move into the
> monorepo.
